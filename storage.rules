// storage.rules
// Cada usuário só acessa sua própria pasta de fotos
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{userId}/fotos/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
